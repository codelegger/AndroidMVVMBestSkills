package com.codelegger.androidmvvmbestskills.core.database

/**
 * Seed for the shared persistence module.
 *
 * The Room database, DAOs, and entities currently live in :app and are intended
 * to be migrated here so all persistence concerns are centralized behind this
 * module's public API.
 */
internal object DatabaseModulePlaceholder
